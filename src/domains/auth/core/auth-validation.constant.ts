export const COGNITO_ADMIN_SCOPE = 'aws.cognito.signin.user.admin';
export const NICKNAME_PATTERN = /^[가-힣A-Za-z0-9]{2,10}$/;
export const PASSWORD_PATTERN = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,256}$/;
